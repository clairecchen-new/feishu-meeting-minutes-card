---
name: feishu-meeting-minutes-card
description: 根据飞书会议纪要文档或会议记录文本，提炼结构化会议纪要并生成飞书 interactive 卡片。触发词：会议纪要、整理会议纪要、会议纪要卡片、会议总结、会议要点、总结一下这个会。
---

# 飞书会议纪要卡片

将飞书会议纪要文档（语音转写或手动记录）整理为结构清晰的飞书 interactive 卡片，并发送到当前会话。

## 触发条件

用户出现以下意图时激活：

- 整理会议纪要、会议总结或会议要点
- 生成会议纪要卡片
- 总结某次会议
- 提供飞书文档 URL，并要求整理或总结
- 直接粘贴会议记录，并要求提炼结构化内容

不要在以下场景触发：

- 用户只要求读取或查看文档原文
- 用户只要求创建或编辑普通飞书文档
- 用户没有要求总结会议内容

## 输入方式

支持两种输入：

1. **飞书文档 URL**：读取链接对应的会议纪要或语音转写内容。
2. **直接粘贴文本**：使用用户提供的会议记录文本。

如果用户提供了飞书链接，使用当前环境中可用的飞书文档读取工具。不同运行环境的工具名称可能不同，不要假定某个特定 MCP 函数一定存在。

## 执行流程

### Step 1：获取会议内容

- 飞书文档 URL：读取完整正文。
- 直接粘贴文本：使用用户提供的全部内容。
- 如果文档无权限或内容为空，明确告知用户，并给出补充权限或粘贴正文的下一步。

### Step 2：提取基本信息

尽可能从原文提取：

- 📅 会议日期、开始时间和时长
- 👥 参会人员或发言人
- 📋 讨论议题
- ✅ 关键决策和共识
- ⚠️ Gap、风险、分歧与待解决问题
- 🧭 Action Items：负责人、任务、截止时间或时间要求

原文未提供的信息不要编造。无法确定时，标记为“原文未注明”或省略对应字段。

### Step 3：重组会议内容

会议记录通常按时间线展开，需要改写为主题线：

1. 识别话题转换点。
2. 合并会议中多次出现的同一主题。
3. 将内容归纳为 3–8 个议题；短会议可少于 3 个。
4. 每个议题优先包含：
   - 背景或现状
   - 关键观点
   - 已达成共识
   - 未解决分歧
   - 明确决定
5. 将跨议题的风险和 Action Items 汇总到卡片末尾。

### Step 4：精炼文字

遵循以下原则：

- 删除“嗯”“对对对”等语气词、重复句和无意义对话。
- 保留关键人名、技术术语、数据、指标和时间要求。
- 保持中性准确，不添加原文没有的推断。
- 每个要点控制在 1–2 句话，尽量不超过 50 个汉字。
- Action Items 必须尽可能明确到人，不使用只有“后续跟进”的模糊表达。
- 如果负责人或截止时间未明确，写明“负责人待确认”或“时间待确认”。

### Step 5：生成并发送飞书卡片

使用当前环境可用的飞书消息工具，构建 `interactive` 类型卡片并发送到当前对话。

发送目标规则：

- 私聊：发送给当前用户。
- 群聊：发送到当前群聊。
- 不要擅自发送到其他用户或群组。
- 如果当前环境无法直接发送卡片，输出完整卡片 JSON，方便用户手动发送或接入。

## 卡片结构与固定版式

默认保持以下版式，不依赖历史会话中的卡片样例。用户明确要求其他布局时再调整。

### Header

- 标题为“会议纪要 | <会议主题>”，副标题紧凑展示会议日期、开始时间和时长；缺失字段省略。
- 每次生成卡片时，从 `green`（绿色）和 `blue`（蓝色）中等概率随机选择一个作为 `header.template`；用户明确指定颜色时遵循用户要求。
- 随机选择仅作用于 Header，不随之改变正文布局、字体层级或正文配色。
- 状态标签仅在原文有明确依据时添加，不复制示例会议的状态、修订号或结论。

### 顶部：三个核心重点

Header 下方首先展示一行三个等宽的重点色块。它们是会议的三个核心重点，不是必须凑齐的三个数字指标。

- 按重要性选择影响决策、交付或后续行动的三个不同重点；有明确数据时优先突出数量、比例、日期或时间节点，但不为展示数字挤掉更重要的定性结论。
- 每块上方使用适度突出的短摘要（`text_size: "heading-4"`，加粗），保持此字号；下方使用单行短说明，按文字长度与列宽选择 `normal` 或 `notation`。优先精炼为约 8–12 个汉字，不手动换行，避免末尾孤字。必要的详细条件放入对应正文。摘要与说明居中，三个色块高度和间距一致。
- 数据例：摘要“8 项”，说明“验收整改事项”；摘要“9/30”，说明“目标交付节点”。这些仅为表达形式示例，不得带入其他会议。
- 无数据也可使用短结论，例如摘要“方案已定”，说明“采用双路供电方案”；仅在原文支持时使用。
- 日期、单位、统计范围和不确定性必须准确；“预计”“目标”“待确认”等限定不能因精简而丢失，不得把目标日期改写为已承诺日期。
- 原文确实不足三个独立重点时，只显示实际有依据的一至两个并均分宽度；不编造数字，不重复同一重点凑数。
- 三块分别采用浅紫、浅蓝、浅橙背景，摘要使用对应较深色，说明使用中性灰；颜色保持固定，不跟随 Header 随机变化。
- 不将此区域替换为“会议信息＋核心结论”两栏。参会人需要展示时，在重点色块后用紧凑的小字一行补充（`text_size: "notation"`，仅“参会/发言”标签加粗），不占用重点色块。

### 正文：独立议题色块

- 每个议题独占一个全宽的浅蓝色背景区域（`blue-50`），使用圆角、充足内边距和模块间留白；不要把全部议题合并为一个大色块。
- 议题标题使用深蓝色、加粗和中文数字编号，如“一、验收与整改闭环”。标题与正文均使用 `text_size: "normal"`，通过颜色和字重区分层级；议题标题不使用 `heading-0` 至 `heading-4`，也不使用 Markdown `#` 标题放大。顶部核心重点的摘要不受此限制。正文为深色文本，主要要点使用蓝色圆点。
- 每个要点保持完整的事实、背景及必要的行动信息，避免为缩短文字而遗漏重要细节。
- 关键 Gap 与待解决问题位于末尾的独立浅红底模块（`red-50`），标题使用深红色；后续 Action Items 使用独立浅灰底模块（`grey-50`），标题使用深灰色。两者标题均保持 `normal` 字号并加粗；没有相关内容时可省略。

结构顺序：

```text
Header：标题 / 时间与时长 / 有依据的状态标签
三个核心重点：[短摘要 + 说明] [短摘要 + 说明] [短摘要 + 说明]
参会人：紧凑展示（可选）
独立议题色块：一、议题标题 + 要点
独立议题色块：二、议题标题 + 要点
……
独立模块：关键 Gap 与待解决问题
独立模块：后续 Action Items
```

### 构建卡片

以上是布局规范，不是可直接发送的 JSON。使用当前环境支持的飞书卡片 schema，将三个重点实现为等宽列，将各议题实现为独立带背景的容器，并构建完整 `interactive` 卡片 JSON。按所用 schema 的文档确认列、背景、圆角、字号与间距字段，不混用不同版本字段。

三个重点保持等宽并列（`column_set.flex_mode: "none"`），不主动改为上下堆叠。发送前按目标显示宽度检查说明长度，优先精简文案，再调整说明字号。原生 Markdown 不保证任意客户端宽度下自动缩字，不要将选定字号宣称为运行时自动适配。若当前工具确实不支持某项装饰样式，保留三个重点和独立议题的结构，并说明具体限制。

## 格式要求

- 默认字号：正文和章节标题使用 `normal`；重点说明按可用宽度选择 `normal` 或 `notation`；参会人和底部链接使用 `notation`。章节标题依靠加粗与颜色突出，顶部三个核心摘要保持 `heading-4` 加粗。保留参会人的小字设计，不升级为独立标题或信息色块。
- 底部仅显示 `[查看会议原文](原文URL)`，不追加修订号、整理依据、状态说明或其他文字；无原文链接时省略，不编造链接。
- 正文优先精炼冗长句子，避免人为断行和末尾孤字；不得为强行单行而删掉关键事实或将长正文缩成难以阅读的小字。

- Header 默认在 `green` 与 `blue` 中等概率随机选择，不固定为红色或其他单一主题；用户明确指定颜色时优先使用指定颜色。
- 议题使用中文数字编号：一、二、三……
- 每个议题使用独立背景容器和留白分隔，分隔线不替代议题色块。
- 主要要点使用圆点列表；仅在需要嵌套细节时使用 `├` / `└`。
- 关键人名使用粗体。
- ⚠️ 用于 Gap、风险和未解决问题。
- ✅ 用于已达成的共识或决议。
- 禁止在卡片正文中使用 Markdown 表格。
- Action Items 按优先级标记：
  - 🔴 紧急 / 本周内
  - 🔵 常规 / 近期
  - 🟢 后续 / 长期

## 质量检查

发送前确认：

- 顶部是否为三个有原文依据的核心重点（信息不足时按实际数量），数据存在时是否优先突出且保留单位和限定。
- 是否保持等宽重点色块、独立议题色块和彩色标题，而非两栏摘要或一个合并正文块。
- 是否只有 Header 主题随机变化，正文布局与配色保持稳定。

- 是否删除了口语化冗余。
- 是否按主题而不是时间线组织内容。
- 是否保留了关键决策、人名、数据和术语。
- 是否区分了共识、分歧和待解决问题。
- Action Items 是否尽可能包含负责人、任务和时间要求。
- 是否避免编造原文不存在的信息。
- 卡片是否发送到当前对话，而不是其他位置。
